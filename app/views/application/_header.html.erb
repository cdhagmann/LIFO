<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <div class="container-fluid">
    <div class='d-flex flex-row justify-content-center align-items-center navbar-brand'>
      <%= link_to 'LIF', root_path, :class => 'h2 py-0 my-0 px-0 mx-0 text-danger font-weight-bold' %>
      <%= image_tag('favicon.png', :size => '30') %>
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <li class="nav-item">
          <%= link_to 'API', api_path, :class => 'text-danger nav-link' %>
        </li>
      </ul>
      <%= form_with(url: questions_path, method: :get, local: true, class: 'd-flex') do |form| %>
        <%= form.text_field :search, class: 'form-control-sm me-2', placeholder: 'Search' %>
        <%= submit_tag 'Search', class: "btn btn-primary my-2 my-sm-0" %>
      <% end %>
    </div>
    <div id='login'>
      <%= render 'login' %>
    </div>
  </div>
</nav>
